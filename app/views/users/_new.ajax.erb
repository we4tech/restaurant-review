$('#registration_form').html("<%= escape_javascript(render(:partial => 'users/new.html.erb')) %>")
$('#registration_form').ajaxify({urlRef: false});

if (pageCallback != undefined) {
  pageCallback.call();
}