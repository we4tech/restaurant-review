.properties
  %table
    - if !just_map
      %tr
        %td{:style => 'font-size: 80%; width: 100px;', :class => 'address'}
          Address
        %td{:style => 'font-size: 80%; padding: 5px;'}
          %b= restaurant.address
    %tr
      %td{:colspan => 2}
        = link_to image_tag(map_url + "295x100"), full_map_url(:rid => restaurant.id),
                :class => 'xopenUpinLightBox',
                :title => link_to(t('fields.map'), details_link)

