<div class='module'>
  <ul class='menu'>
    <% menu.each do |menu_ref| %>
      <li class='<%= menu_ref[:active] ? 'active' : '' %>'>
        <%= link_to menu_ref[:label], menu_ref[:link] %>
      </li>
    <% end %>
  </ul>
</div>